---
phase: 02-reviews-meals
plan: 05
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - apps/web/components/review-card.tsx
  - apps/web/components/floating-action-button.tsx
  - apps/web/components/delete-dialog.tsx
  - apps/web/app/(app)/reviews/page.tsx
  - apps/web/app/(app)/reviews/[id]/edit/page.tsx
autonomous: true
requirements: [REVW-06, REVW-07]

must_haves:
  truths:
    - "User sees their reviews in a reverse chronological list at /reviews"
    - "Each review card shows photo, restaurant name, rating, note preview, tags, and meal date"
    - "User can tap edit on a review card and navigate to the edit form"
    - "User can tap delete, confirm in a dialog, and the review disappears from the list"
    - "Empty state shows 'No reviews yet' message when user has no reviews"
    - "FAB is visible on the review list, linking to /reviews/new"
  artifacts:
    - path: "apps/web/components/review-card.tsx"
      provides: "Review card display component"
      contains: "ReviewCard"
    - path: "apps/web/components/floating-action-button.tsx"
      provides: "FAB component for review creation"
      contains: "FloatingActionButton"
    - path: "apps/web/components/delete-dialog.tsx"
      provides: "Delete confirmation dialog"
      contains: "DeleteDialog"
    - path: "apps/web/app/(app)/reviews/page.tsx"
      provides: "My reviews list page"
      contains: "reviews"
    - path: "apps/web/app/(app)/reviews/[id]/edit/page.tsx"
      provides: "Edit review page"
      contains: "ReviewComposer"
  key_links:
    - from: "apps/web/app/(app)/reviews/page.tsx"
      to: "/api/v1/reviews"
      via: "GET request to fetch user's reviews"
      pattern: "api/v1/reviews"
    - from: "apps/web/components/review-card.tsx"
      to: "/api/v1/reviews/[id]"
      via: "DELETE request on delete confirmation"
      pattern: "api/v1/reviews"
    - from: "apps/web/app/(app)/reviews/[id]/edit/page.tsx"
      to: "apps/web/components/review-composer.tsx"
      via: "ReviewComposer in edit mode with initialData"
      pattern: "ReviewComposer.*mode.*edit"
---

<objective>
Build the review list page (/reviews), review card component, edit page, delete confirmation dialog, and floating action button. This completes the post-submit landing (D-09) and edit/delete flows (REVW-06, REVW-07).

Purpose: Users need to see their reviews after posting (D-09) and be able to edit or delete them. The FAB provides persistent access to create new reviews (D-02).
Output: Three components and two page routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reviews-meals/02-CONTEXT.md
@.planning/phases/02-reviews-meals/02-UI-SPEC.md

<interfaces>
From Plan 04 (review-composer.tsx):
```typescript
// ReviewComposer accepts:
// mode: 'create' | 'edit'
// initialData?: Partial<CreateReviewInput> & { id?: string }
// onSuccess?: () => void
```

From Plan 03 (GET /api/v1/reviews response shape):
```typescript
// Array of review objects:
{
  id: string;
  userId: string;
  restaurantId: string | null;
  mealType: 'restaurant' | 'homemade';
  body: string | null;
  rating: string | null;  // numeric stored as string by Drizzle
  photoUrl: string | null;
  mealDate: string | null;  // date column returns string
  deletedAt: null;  // always null (filtered)
  createdAt: string;
  updatedAt: string;
  tags?: string[];
  restaurant?: { name: string; address: string | null } | null;
}
```

From Plan 04 (star-rating.tsx):
```typescript
// StarRating accepts: value, onChange, readOnly?, size?
// size 'sm' = 20px stars for card display
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ReviewCard, FloatingActionButton, and DeleteDialog components</name>
  <files>apps/web/components/review-card.tsx, apps/web/components/floating-action-button.tsx, apps/web/components/delete-dialog.tsx</files>
  <read_first>
    - apps/web/components/star-rating.tsx (confirm StarRating readOnly mode props for card display)
    - apps/web/components/auth-card.tsx (existing component pattern — Tailwind classes, import style)
    - packages/shared/src/constants/tokens.ts (design tokens for colors, spacing, radii)
    - .planning/phases/02-reviews-meals/02-UI-SPEC.md (Component #6 ReviewCard, #7 FAB, delete dialog specs)
  </read_first>
  <action>
**1. `apps/web/components/review-card.tsx`** — per UI-SPEC Component #6:
- `'use client'` directive
- Props: `review: { id: string; body: string | null; rating: string | null; photoUrl: string | null; mealType: string; mealDate: string | null; restaurant?: { name: string; address: string | null } | null; tags?: string[] }`, `onEdit: (id: string) => void`, `onDelete: (id: string) => void`
- Surface bg (#FFF0E0) card, border (#E7D5C5), border-radius 12px (radii.lg), shadow: `0 1px 4px 0 rgba(28,25,23,0.08)`
- Layout top-to-bottom:
  1. Photo thumbnail (if present): 100% width, 160px height, `object-cover`, rounded top corners only. Use `<img>` tag (not Next.js Image to avoid SSR complexity for now — or Next.js Image with the configured remotePatterns from Plan 02)
  2. Restaurant name (16px/semibold/text-primary) + meal type badge only for homemade ("Homemade" — 14px/regular/text-secondary, surface bg, border, border-radius sm)
  3. Rating stars: import `<StarRating value={parseFloat(review.rating)} readOnly size="sm" />` — 20px stars, read-only
  4. Review note: 14px/regular/text-primary, 1.5 line-height, CSS line-clamp at 3 lines, "Show more" toggle button (14px/accent) to expand
  5. Tags row: comma-separated or chip display, 14px/text-secondary
  6. Bottom row: meal date formatted as "Apr 29, 2026" (14px/text-secondary) + kebab menu (three dots ··· button) top-right of card
- Kebab menu: popover or dropdown with two items:
  - Pencil icon (lucide-react, 16px) + "Edit" label → calls `onEdit(review.id)`
  - Trash2 icon (lucide-react, 16px) + "Delete" label in destructive color → calls `onDelete(review.id)`
- Format meal date: `new Date(review.mealDate + 'T00:00:00').toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })` — the `T00:00:00` suffix prevents timezone shift since mealDate is a date string without time
- Card hover (web): border-accent (no transform)

**2. `apps/web/components/floating-action-button.tsx`** — per UI-SPEC Component #7:
- Props: `href: string` (defaults to '/reviews/new')
- Fixed position: bottom-6 right-6 (24px), z-50
- 56x56px circular button, accent bg (#F97316), white Plus icon (lucide-react, 24px)
- Shadow: `0 4px 12px rgba(249,115,22,0.35)`
- Hover: accent-hover bg. Active: accent-active bg + scale-95
- `aria-label="Write a review"`
- Render as Next.js `<Link>` to href

**3. `apps/web/components/delete-dialog.tsx`** — per UI-SPEC delete confirmation:
- Props: `open: boolean`, `onClose: () => void`, `onConfirm: () => void`, `loading?: boolean`
- Render as a modal overlay with `role="dialog"`, focus-trapped
- Title: "Delete this review?" — 20px/semibold
- Body: "This can't be undone." — 16px/regular/text-secondary
- Cancel button: "Keep Review" — outlined, border-border, text-primary
- Confirm button: "Delete" — destructive red bg (#EF4444), white text
- Both buttons: 44px tall, full width stacked on mobile
- Backdrop: semi-transparent dark overlay, clicking backdrop calls onClose
- When loading: Loader2 spinner in confirm button, both buttons disabled
  </action>
  <verify>
    <automated>cd /Users/sydneycole/Desktop/lunchboxd && pnpm --filter web type-check</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/review-card.tsx contains `'use client'`
    - apps/web/components/review-card.tsx contains `StarRating` (imported for read-only display)
    - apps/web/components/review-card.tsx contains `line-clamp` (3-line note clamping)
    - apps/web/components/review-card.tsx contains `Pencil` and `Trash2` (lucide-react icons)
    - apps/web/components/review-card.tsx contains `Homemade` (badge text)
    - apps/web/components/floating-action-button.tsx contains `aria-label` with `Write a review`
    - apps/web/components/floating-action-button.tsx contains `fixed` (CSS positioning)
    - apps/web/components/floating-action-button.tsx contains `#F97316` or `accent` (color reference)
    - apps/web/components/delete-dialog.tsx contains `role="dialog"`
    - apps/web/components/delete-dialog.tsx contains `Delete this review?`
    - apps/web/components/delete-dialog.tsx contains `Keep Review`
    - pnpm --filter web type-check exits 0
  </acceptance_criteria>
  <done>ReviewCard displays all review data with kebab menu for edit/delete. FAB links to /reviews/new with correct sizing and accessibility. DeleteDialog provides confirmation with cancel/confirm buttons and loading state.</done>
</task>

<task type="auto">
  <name>Task 2: Build review list page (/reviews) and edit page (/reviews/[id]/edit)</name>
  <files>apps/web/app/(app)/reviews/page.tsx, apps/web/app/(app)/reviews/[id]/edit/page.tsx</files>
  <read_first>
    - apps/web/components/review-card.tsx (Task 1 output — confirm ReviewCard props interface)
    - apps/web/components/floating-action-button.tsx (Task 1 output — confirm export)
    - apps/web/components/delete-dialog.tsx (Task 1 output — confirm DeleteDialog props)
    - apps/web/components/review-composer.tsx (Plan 04 output — confirm ReviewComposer edit mode props)
    - apps/web/app/(app)/layout.tsx (confirm auth gate exists)
    - .planning/phases/02-reviews-meals/02-UI-SPEC.md (Screen B: Review List spec, Screen C: Edit Review spec)
  </read_first>
  <action>
**1. `apps/web/app/(app)/reviews/page.tsx`** — per UI-SPEC Screen B:
- `'use client'` directive (needs TanStack Query for data fetching and interactive delete)
- Fetch reviews via `useQuery({ queryKey: ['my-reviews'], queryFn: () => fetch('/api/v1/reviews').then(r => r.json()) })`
- Layout: single column, max-w-[600px] mx-auto, bg (#FFF8F0), py-8 (xl)
- Header: "My Reviews" heading (20px/semibold/text-primary) with review count "(12)" in text-secondary
- List: map reviews to `<ReviewCard>` components, 24px gap between cards
- `<FloatingActionButton href="/reviews/new" />` — fixed bottom-right
- Empty state (when reviews array is empty):
  - Centered: empty bowl icon placeholder (48px, text-secondary — use a simple SVG or lucide-react UtensilsCrossed icon)
  - "No reviews yet" — 20px/semibold/text-primary
  - "Tap + to log your first meal." — 16px/regular/text-secondary
  - No CTA button (FAB is visible)
- Delete flow:
  1. ReviewCard's onDelete callback sets the target review ID in state
  2. Opens `<DeleteDialog>`
  3. On confirm: `fetch(\`/api/v1/reviews/${id}\`, { method: 'DELETE' })` then invalidate query
  4. On success: close dialog, review disappears from list
  5. Use `useMutation` from TanStack Query for the delete request with `onSuccess: () => queryClient.invalidateQueries({ queryKey: ['my-reviews'] })`
- Edit flow: `router.push(\`/reviews/${id}/edit\`)` on ReviewCard's onEdit callback
- Loading state: show skeleton or spinner while reviews load

**2. `apps/web/app/(app)/reviews/[id]/edit/page.tsx`** — per UI-SPEC Screen C:
- `'use client'` directive
- Fetch the single review: `useQuery({ queryKey: ['review', id], queryFn: () => fetch(\`/api/v1/reviews\`).then(r => r.json()).then(reviews => reviews.find(r => r.id === id)) })`
  - Note: there's no single-review GET endpoint, so fetch all and filter by id. (Alternatively, the executor can add a GET /api/v1/reviews/[id] endpoint if preferred — this is fine as long as it filters deletedAt.)
- Use the `id` from the URL: extract via `useParams()` from `next/navigation`
- Render `<ReviewComposer mode="edit" initialData={{ id, ...existingReview }} onSuccess={() => router.push('/reviews')} />`
- Page title handled by ReviewComposer (it shows "Edit Review" in edit mode)
- Below the submit button: "Delete review" text button — 14px/semibold/destructive, right-aligned
- Clicking "Delete review" opens `<DeleteDialog>`, on confirm: DELETE then redirect to /reviews
- Loading state while fetching existing review data
  </action>
  <verify>
    <automated>cd /Users/sydneycole/Desktop/lunchboxd && pnpm --filter web type-check</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(app)/reviews/page.tsx contains `'use client'`
    - apps/web/app/(app)/reviews/page.tsx contains `useQuery` (TanStack Query)
    - apps/web/app/(app)/reviews/page.tsx contains `my-reviews` (query key)
    - apps/web/app/(app)/reviews/page.tsx contains `ReviewCard`
    - apps/web/app/(app)/reviews/page.tsx contains `FloatingActionButton`
    - apps/web/app/(app)/reviews/page.tsx contains `No reviews yet`
    - apps/web/app/(app)/reviews/page.tsx contains `DeleteDialog`
    - apps/web/app/(app)/reviews/page.tsx contains `api/v1/reviews` (fetch call)
    - apps/web/app/(app)/reviews/[id]/edit/page.tsx contains `'use client'`
    - apps/web/app/(app)/reviews/[id]/edit/page.tsx contains `ReviewComposer`
    - apps/web/app/(app)/reviews/[id]/edit/page.tsx contains `mode` with value `edit`
    - pnpm --filter web type-check exits 0
  </acceptance_criteria>
  <done>Review list page at /reviews shows user's reviews in reverse chronological order with empty state, FAB, and delete flow. Edit page at /reviews/[id]/edit pre-populates ReviewComposer in edit mode and includes delete action. Both pages are within the authenticated (app) layout.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web type-check` passes
- `/reviews` route exists and renders ReviewCard list
- `/reviews/new` route exists (from Plan 04) and renders composer
- `/reviews/[id]/edit` route exists and renders composer in edit mode
- FAB is visible on the review list page
- Delete dialog renders with correct copy and confirms deletion
- Empty state shows when user has no reviews
</verification>

<success_criteria>
- User sees their reviews at /reviews in reverse chronological order per D-09
- Review cards display photo, restaurant, rating, note, tags, date per UI-SPEC
- User can edit via kebab menu, navigating to pre-populated edit form per REVW-06
- User can delete via kebab menu or edit page, with confirmation dialog per REVW-07
- FAB always visible on review list per D-02
</success_criteria>

<output>
After completion, create `.planning/phases/02-reviews-meals/02-05-SUMMARY.md`
</output>
