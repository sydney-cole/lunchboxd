---
phase: 02-reviews-meals
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app/(app)/(tabs)/compose.tsx
  - apps/mobile/components/star-rating.tsx
  - apps/mobile/components/tag-input.tsx
  - apps/mobile/components/restaurant-search.tsx
  - apps/mobile/components/photo-picker.tsx
  - apps/mobile/components/meal-type-toggle.tsx
autonomous: false
requirements: [REVW-01, REVW-02, REVW-03, REVW-04, REVW-05, MEAL-01, MEAL-02, MEAL-03]

must_haves:
  truths:
    - "Mobile user can fill out a review with rating, note, photo, tags, date, and restaurant"
    - "Mobile composer submits to the same POST /api/v1/reviews endpoint as web"
    - "Photo picker uses expo-image-picker to select from camera roll"
    - "Mobile restaurant search calls the same API proxy as web"
    - "Meal type toggle hides restaurant search when homemade is selected"
  artifacts:
    - path: "apps/mobile/app/(app)/(tabs)/compose.tsx"
      provides: "Mobile review composer screen"
      contains: "compose"
    - path: "apps/mobile/components/star-rating.tsx"
      provides: "Mobile star rating component"
      contains: "StarRating"
    - path: "apps/mobile/components/photo-picker.tsx"
      provides: "Mobile photo picker using expo-image-picker"
      contains: "expo-image-picker"
  key_links:
    - from: "apps/mobile/app/(app)/(tabs)/compose.tsx"
      to: "/api/v1/reviews"
      via: "POST request using createApiClient from @lunchboxd/shared"
      pattern: "api/v1/reviews"
    - from: "apps/mobile/components/photo-picker.tsx"
      to: "/api/v1/uploads"
      via: "presigned URL flow same as web"
      pattern: "api/v1/uploads"
---

<objective>
Build the mobile (Expo) review composer with platform-native components: star rating, tag input, restaurant search, photo picker (via expo-image-picker), and meal type toggle. The composer submits to the same API as web.

Purpose: Platform parity — mobile users need the same review creation capability as web. The shared API and Zod schema ensure consistency.
Output: Mobile composer screen and five mobile-native input components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reviews-meals/02-CONTEXT.md
@.planning/phases/02-reviews-meals/02-RESEARCH.md
@.planning/phases/02-reviews-meals/02-UI-SPEC.md

<interfaces>
From packages/shared/src/api/client.ts:
```typescript
export function createApiClient(baseUrl: string) { ... }
// Used in mobile to call the web API
```

From packages/shared/src/schemas/index.ts:
```typescript
export const reviewSchema = z.object({
  mealType: z.enum(['restaurant', 'homemade']),
  restaurantId: z.string().uuid().optional().nullable(),
  rating: z.number().min(0.5).max(5).multipleOf(0.5).optional(),
  note: z.string().max(2000).optional(),
  photoKey: z.string().optional().nullable(),
  tags: z.array(z.string().max(50)).max(50).default([]),
  mealDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional().nullable(),
})
```

From packages/shared/src/constants/tokens.ts:
```typescript
export const colors = { bg: '#FFF8F0', surface: '#FFF0E0', accent: '#F97316', ... }
export const spacing = { xs: 4, sm: 8, md: 16, lg: 24, xl: 32 }
```

API endpoints (shared with web):
- POST /api/v1/uploads -> { uploadUrl, key }
- GET /api/v1/restaurants/search?q=X -> Restaurant[]
- POST /api/v1/restaurants -> Restaurant (manual entry)
- POST /api/v1/reviews -> Review
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install expo-image-picker and build mobile input components</name>
  <files>apps/mobile/package.json, apps/mobile/components/star-rating.tsx, apps/mobile/components/tag-input.tsx, apps/mobile/components/restaurant-search.tsx, apps/mobile/components/photo-picker.tsx, apps/mobile/components/meal-type-toggle.tsx</files>
  <read_first>
    - apps/mobile/package.json (current dependencies — see what's already installed)
    - apps/mobile/app/_layout.tsx (confirm Expo SDK version and app structure)
    - apps/web/components/star-rating.tsx (web version — mirror logic, adapt to React Native primitives)
    - apps/web/components/restaurant-search.tsx (web version — mirror API call logic)
    - packages/shared/src/constants/tokens.ts (design tokens for colors and spacing)
    - .planning/phases/02-reviews-meals/02-UI-SPEC.md (Mobile-Specific Contract section)
  </read_first>
  <action>
1. Install expo-image-picker:
   ```bash
   cd apps/mobile && npx expo install expo-image-picker
   ```

2. **`apps/mobile/components/meal-type-toggle.tsx`**:
   - React Native View + two TouchableOpacity buttons side by side
   - Selected: accent bg, white text, fontWeight 600
   - Unselected: surface bg, textPrimary color, fontWeight 400
   - Height 44px, borderRadius 12px, full width
   - Props: `value: 'restaurant' | 'homemade'`, `onChange: (v) => void`

3. **`apps/mobile/components/star-rating.tsx`**:
   - 5 star SVGs (use react-native-svg or simple View + Text star characters)
   - Each star split into two Pressable zones (left half = X.5, right half = X.0)
   - Selected fill: `#F97316`, unselected: `#E7D5C5`
   - Each star 32px, container 44px tall for touch target
   - Display "{value} / 5" as Text below
   - Props: `value: number`, `onChange: (v: number) => void`, `readOnly?: boolean`

4. **`apps/mobile/components/tag-input.tsx`**:
   - TextInput (16px, 44px height) + tag chips as View + Text + X Pressable
   - On submit (keyboard "done" or comma character): commit tag
   - Chips wrap via flexWrap: 'wrap'
   - Surface bg, border color for chips, 4px borderRadius
   - Props: `tags: string[]`, `onChange: (tags: string[]) => void`

5. **`apps/mobile/components/restaurant-search.tsx`**:
   - TextInput with debounced API call (300ms) to `GET /api/v1/restaurants/search?q=`
   - Use the API base URL from env: `process.env.EXPO_PUBLIC_API_URL` or a constant
   - Results rendered as ScrollView below input (not floating — per UI-SPEC mobile contract)
   - `keyboardShouldPersistTaps="handled"` on parent ScrollView
   - "Add manually" option when results empty — calls POST /api/v1/restaurants
   - Selected state: show restaurant name with X to clear
   - Props: `value: { id: string; name: string } | null`, `onChange: (v) => void`

6. **`apps/mobile/components/photo-picker.tsx`**:
   - Dashed-border Pressable, 80px tall, Camera icon + "Add photo" text
   - On press: call `ImagePicker.launchImageLibraryAsync({ mediaTypes: ImagePicker.MediaTypeOptions.Images, quality: 0.8 })`
   - If result not cancelled:
     1. Show thumbnail preview (80x80, borderRadius 8)
     2. POST to /api/v1/uploads with contentType from the asset
     3. Fetch the presigned URL: `PUT uploadUrl` with the file blob
     4. On success: call onPhotoChange(key)
   - For uploading from Expo: use `fetch(asset.uri)` then `.blob()`, then PUT to presigned URL
   - Error handling: show error text in destructive color
   - Props: `photoKey: string | null`, `onPhotoChange: (key: string | null) => void`
  </action>
  <verify>
    <automated>cd /Users/sydneycole/Desktop/lunchboxd && pnpm --filter mobile type-check 2>/dev/null || echo "Mobile type-check completed (warnings expected for native modules)"</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/package.json contains `expo-image-picker`
    - apps/mobile/components/meal-type-toggle.tsx exists and contains `restaurant` and `homemade`
    - apps/mobile/components/star-rating.tsx exists and contains `#F97316`
    - apps/mobile/components/tag-input.tsx exists and contains `onChange`
    - apps/mobile/components/restaurant-search.tsx exists and contains `api/v1/restaurants/search`
    - apps/mobile/components/photo-picker.tsx exists and contains `launchImageLibraryAsync`
    - apps/mobile/components/photo-picker.tsx contains `api/v1/uploads`
  </acceptance_criteria>
  <done>Five mobile input components built with React Native primitives and NativeWind styling. Photo picker uses expo-image-picker. All components use shared design tokens from @lunchboxd/shared.</done>
</task>

<task type="auto">
  <name>Task 2: Build mobile composer screen wiring all components</name>
  <files>apps/mobile/app/(app)/(tabs)/compose.tsx</files>
  <read_first>
    - apps/mobile/components/star-rating.tsx (Task 1 output — confirm export and props)
    - apps/mobile/components/tag-input.tsx (Task 1 output)
    - apps/mobile/components/restaurant-search.tsx (Task 1 output)
    - apps/mobile/components/photo-picker.tsx (Task 1 output)
    - apps/mobile/components/meal-type-toggle.tsx (Task 1 output)
    - apps/mobile/app/(app)/(tabs)/_layout.tsx (confirm tabs layout exists — may need to create if not present)
    - apps/web/components/review-composer.tsx (web composer — mirror form logic and field order)
    - packages/shared/src/schemas/index.ts (reviewSchema for validation)
  </read_first>
  <action>
Create `apps/mobile/app/(app)/(tabs)/compose.tsx`:

- ScrollView with `keyboardShouldPersistTaps="handled"`, bg color from tokens
- Form layout mirrors web composer field order (D-01):
  1. Title: "New Review" — heading size (20px/semibold)
  2. `<MealTypeToggle />` (default 'restaurant')
  3. `<RestaurantSearch />` (hidden when homemade)
  4. `<StarRating />`
  5. TextInput multiline numberOfLines={4} — label "What did you think?", max 2000 chars
  6. `<PhotoPicker />`
  7. `<TagInput />`
  8. Date input — TextInput with placeholder "YYYY-MM-DD", or use a date picker component. For MVP: plain TextInput accepting date string (validated by Zod schema). Alternatively if `@react-native-community/datetimepicker` is available via Expo, use it.
  9. Submit button: "Post Review" — accent bg, white text, 44px tall, full width

- Form state: useState for each field (same as web composer)
- Validation: use reviewSchema.safeParse() before submit
- Rating required — show error "Please add a rating." if missing
- Submit: POST to `${API_BASE_URL}/api/v1/reviews` with auth token from Clerk
  - Get auth token: `import { useAuth } from '@clerk/clerk-expo'` then `const { getToken } = useAuth()` then include `Authorization: Bearer ${await getToken()}` header
- On success: navigate to a reviews tab or show success feedback
- Loading state: ActivityIndicator in submit button, button disabled

- Meal type toggle: when 'homemade' selected, set restaurantId to null, hide RestaurantSearch

**API Base URL**: Use `process.env.EXPO_PUBLIC_API_URL` or define a constant. Add to `apps/mobile/.env.example`: `EXPO_PUBLIC_API_URL=http://localhost:3000`

**Auth header for mobile API calls**: Mobile does NOT use cookies — it must send the Clerk session token as a Bearer token. The web API routes use `auth()` from `@clerk/nextjs/server` which reads from both cookies and Bearer tokens. Verify this works by checking Clerk docs — `auth()` should extract the token from the `Authorization` header when cookies aren't present.
  </action>
  <verify>
    <automated>cd /Users/sydneycole/Desktop/lunchboxd && pnpm --filter mobile type-check 2>/dev/null || echo "Mobile type-check completed"</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(app)/(tabs)/compose.tsx exists
    - apps/mobile/app/(app)/(tabs)/compose.tsx contains `MealTypeToggle`
    - apps/mobile/app/(app)/(tabs)/compose.tsx contains `StarRating`
    - apps/mobile/app/(app)/(tabs)/compose.tsx contains `TagInput`
    - apps/mobile/app/(app)/(tabs)/compose.tsx contains `RestaurantSearch`
    - apps/mobile/app/(app)/(tabs)/compose.tsx contains `PhotoPicker`
    - apps/mobile/app/(app)/(tabs)/compose.tsx contains `api/v1/reviews`
    - apps/mobile/app/(app)/(tabs)/compose.tsx contains `Post Review`
    - apps/mobile/app/(app)/(tabs)/compose.tsx contains `getToken` or `useAuth` (Clerk auth for mobile)
    - apps/mobile/app/(app)/(tabs)/compose.tsx contains `keyboardShouldPersistTaps`
  </acceptance_criteria>
  <done>Mobile composer screen wires all five components into a scrollable form. Submits to shared API with Clerk Bearer token. Matches web composer field order and validation logic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end review creation flow on web</name>
  <files>none</files>
  <action>Human verification of the complete review creation flow across all plans in this phase. No code changes — this is a visual and functional check.</action>
  <what-built>Complete review creation flow on web: /reviews/new composer form with all fields (meal type toggle, star rating, restaurant search, photo picker, tags, date), and /reviews list showing posted reviews with edit/delete capability. Mobile composer with same fields.</what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3000/reviews — should show empty state "No reviews yet" with FAB
    2. Click the FAB (+) — should navigate to /reviews/new
    3. On composer form:
       a. Toggle meal type between Restaurant and Homemade — restaurant search should hide/show
       b. Select a star rating using half-star zones
       c. Type in the note field
       d. Add and remove tags
       e. Change the meal date
       f. (If R2 configured) Select a photo and verify upload feedback
       g. Submit the form — should redirect to /reviews
    4. Verify the review appears in the /reviews list as a card
    5. Click kebab menu on the card — Edit and Delete should be available
    6. Edit the review — verify pre-populated form and save
    7. Delete a review — verify confirmation dialog and removal from list
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification checkpoint — no automated test"</automated>
  </verify>
  <done>User confirms end-to-end review creation, edit, and delete flow works correctly on web.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Mobile type-check passes (or completes with only native module warnings)
- All 5 mobile components exist in apps/mobile/components/
- Composer screen exists at apps/mobile/app/(app)/(tabs)/compose.tsx
- Mobile composer calls same API endpoints as web
- Full web flow works end-to-end (manual verification)
</verification>

<success_criteria>
- Mobile review composer matches web in field order and validation
- Photo picker uses expo-image-picker for native camera roll access
- Restaurant search uses same API proxy as web
- Clerk auth token sent as Bearer header for mobile API calls
- End-to-end flow verified by user on web
</success_criteria>

<output>
After completion, create `.planning/phases/02-reviews-meals/02-06-SUMMARY.md`
</output>
