---
phase: 02-reviews-meals
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - apps/web/components/star-rating.tsx
  - apps/web/components/tag-input.tsx
  - apps/web/components/restaurant-search.tsx
  - apps/web/components/photo-picker.tsx
  - apps/web/components/meal-type-toggle.tsx
  - apps/web/components/review-composer.tsx
  - apps/web/app/(app)/reviews/new/page.tsx
autonomous: true
requirements: [REVW-01, REVW-02, REVW-03, REVW-04, REVW-05, MEAL-01, MEAL-02, MEAL-03]

must_haves:
  truths:
    - "User can select a half-star rating from 0.5 to 5.0 using interactive star icons"
    - "User can type free-text tags and commit them with Enter or comma"
    - "User can search for a restaurant via inline autocomplete with debounced API calls"
    - "User can select a photo that uploads immediately to R2 via presigned URL"
    - "User can toggle between Restaurant and Homemade meal types"
    - "Homemade toggle hides the restaurant search field"
    - "The full composer form submits a review via POST /api/v1/reviews"
    - "After successful submit, user is redirected to /reviews"
  artifacts:
    - path: "apps/web/components/star-rating.tsx"
      provides: "Half-star interactive rating component"
      contains: "StarRating"
    - path: "apps/web/components/tag-input.tsx"
      provides: "Free-text tag input with chip display"
      contains: "TagInput"
    - path: "apps/web/components/restaurant-search.tsx"
      provides: "Autocomplete restaurant search with manual entry fallback"
      contains: "RestaurantSearch"
    - path: "apps/web/components/photo-picker.tsx"
      provides: "Photo picker with presigned URL upload"
      contains: "PhotoPicker"
    - path: "apps/web/components/meal-type-toggle.tsx"
      provides: "Segmented control for restaurant vs homemade"
      contains: "MealTypeToggle"
    - path: "apps/web/components/review-composer.tsx"
      provides: "Full review composer form wiring all components"
      contains: "ReviewComposer"
    - path: "apps/web/app/(app)/reviews/new/page.tsx"
      provides: "Review composer page route"
      contains: "ReviewComposer"
  key_links:
    - from: "apps/web/components/restaurant-search.tsx"
      to: "/api/v1/restaurants/search"
      via: "fetch with debounced query param"
      pattern: "api/v1/restaurants/search"
    - from: "apps/web/components/photo-picker.tsx"
      to: "/api/v1/uploads"
      via: "POST for presigned URL, then PUT to R2"
      pattern: "api/v1/uploads"
    - from: "apps/web/components/review-composer.tsx"
      to: "/api/v1/reviews"
      via: "POST on form submit"
      pattern: "api/v1/reviews"
---

<objective>
Build the web review composer UI: five input components (star rating, tag input, restaurant search, photo picker, meal type toggle) and the composer form page that wires them together. The form submits to POST /api/v1/reviews and redirects to /reviews on success.

Purpose: This is the primary user action — creating a meal review. All five input components and the form page are needed for a complete review creation experience per D-01 (single full-page scrollable form).
Output: Six component files and one page route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reviews-meals/02-CONTEXT.md
@.planning/phases/02-reviews-meals/02-RESEARCH.md
@.planning/phases/02-reviews-meals/02-UI-SPEC.md

<interfaces>
From packages/shared/src/constants/tokens.ts:
```typescript
export const colors = {
  bg: '#FFF8F0', surface: '#FFF0E0', accent: '#F97316',
  accentHover: '#EA6C10', accentActive: '#D96209',
  textPrimary: '#1C1917', textSecondary: '#78716C',
  border: '#E7D5C5', destructive: '#EF4444', white: '#FFFFFF',
}
export const spacing = { xs: 4, sm: 8, md: 16, lg: 24, xl: 32 }
export const fontSizes = { label: 14, body: 16, heading: 20, display: 32 }
export const radii = { sm: 4, md: 8, lg: 12 }
```

From packages/shared/src/schemas/index.ts (after Plan 01):
```typescript
export const reviewSchema = z.object({
  mealType: z.enum(['restaurant', 'homemade']),
  restaurantId: z.string().uuid().optional().nullable(),
  rating: z.number().min(0.5).max(5).multipleOf(0.5).optional(),
  note: z.string().max(2000).optional(),
  photoKey: z.string().optional().nullable(),
  tags: z.array(z.string().max(50)).max(50).default([]),
  mealDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional().nullable(),
})
export type CreateReviewInput = z.infer<typeof reviewSchema>
```

API endpoints (from Plans 02, 03):
- POST /api/v1/uploads → { uploadUrl: string, key: string }
- GET /api/v1/restaurants/search?q=X → Restaurant[]
- POST /api/v1/restaurants → Restaurant (manual entry)
- POST /api/v1/reviews → Review (create)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build five input components (StarRating, TagInput, RestaurantSearch, PhotoPicker, MealTypeToggle)</name>
  <files>apps/web/components/star-rating.tsx, apps/web/components/tag-input.tsx, apps/web/components/restaurant-search.tsx, apps/web/components/photo-picker.tsx, apps/web/components/meal-type-toggle.tsx</files>
  <read_first>
    - apps/web/components/auth-card.tsx (existing component pattern — 'use client' directive, Tailwind class conventions, import style)
    - apps/web/app/globals.css (Tailwind v4 theme — confirm CSS custom properties available for colors)
    - packages/shared/src/constants/tokens.ts (design tokens — colors, spacing, radii values)
    - .planning/phases/02-reviews-meals/02-UI-SPEC.md (exact visual specs for each component)
  </read_first>
  <action>
All five components are `'use client'` Client Components using Tailwind CSS v4 classes.

**1. `apps/web/components/star-rating.tsx`** — per UI-SPEC Component #2:
- Props: `value: number`, `onChange: (value: number) => void`, `readOnly?: boolean`, `size?: 'sm' | 'md'`
- Render 5 SVG star icons, each 32px (md) or 20px (sm), each split into left-half and right-half click zones
- Left half click = X.5 stars (e.g., star 3 left = 2.5), right half click = X.0 stars (e.g., star 3 right = 3.0)
- Selected stars: fill `#F97316` (accent). Unselected: fill `#E7D5C5` (border color)
- Partially filled stars: use CSS `clip-path` or two layered SVGs (filled background + empty foreground) to show half-fill
- Display `"{value} / 5"` below in 14px / text-secondary
- `role="radiogroup"`, each half-zone is `role="radio"` with `aria-label="X.5 stars"` or `aria-label="X stars"`
- Keyboard: left arrow decreases by 0.5, right arrow increases by 0.5
- Touch zones: each half-star zone minimum 22px wide, total star container 44px tall (padding for touch accessibility)
- When `readOnly`, no click handlers, no role="radiogroup", just visual display

**2. `apps/web/components/tag-input.tsx`** — per UI-SPEC Component #4:
- Props: `tags: string[]`, `onChange: (tags: string[]) => void`
- Text input (16px, 44px tall) — on Enter or comma key, commit current input as a new tag
- Committed tags render as chips above the input: surface bg (#FFF0E0), border (#E7D5C5), border-radius 4px (radii.sm), 14px text, 4px vertical / 8px horizontal padding
- Each chip has an X close button (lucide-react X icon, 16px) — clicking removes the tag
- Chips wrap to new lines
- Input placeholder: "Add a tag (e.g. comfort food, date night)"
- Trim whitespace, ignore empty strings
- Unlimited tags per D-08

**3. `apps/web/components/restaurant-search.tsx`** — per UI-SPEC Component #3:
- Props: `value: { id: string; name: string } | null`, `onChange: (restaurant: { id: string; name: string } | null) => void`
- Text input (16px, 44px tall, surface bg, border-border)
- Debounce 300ms — on input change, call `GET /api/v1/restaurants/search?q=${encodeURIComponent(query)}`
- Use `useState` + `useEffect` with clearTimeout for debounce (or TanStack Query with `enabled: query.length >= 2`)
- Dropdown: max 5 results, each row 44px min height, surface bg (#FFF0E0), border (#E7D5C5), border-radius 8px
- Each result: restaurant name (16px / text-primary), address below (14px / text-secondary)
- Loading state: Loader2 spinner (lucide-react, 16px) inside input on the right
- "Add manually" row: appears ONLY when search returns zero results (D-05) — "+"Add '[query]' manually" in 14px accent color
- On manual entry select: call `POST /api/v1/restaurants` with `{ name: query }`, use returned restaurant as selected value
- On restaurant select from dropdown: set value to `{ id: restaurant.id, name: restaurant.name }`
- Selected state: show restaurant name as plain text with X to clear
- `role="listbox"` on dropdown, `role="option"` on each result, `aria-expanded` and `aria-haspopup="listbox"` on input

**4. `apps/web/components/photo-picker.tsx`** — per UI-SPEC Component #5:
- Props: `photoKey: string | null`, `onPhotoChange: (key: string | null) => void`
- Default state: dashed border button, surface bg, 80px tall, full width, centered Camera icon (lucide-react, 24px) + "Add photo" label (16px)
- On file select via hidden `<input type="file" accept="image/jpeg,image/png,image/webp">`:
  1. Validate file size <= 10MB client-side. If over, show error: "Photo must be under 10MB."
  2. Create object URL for thumbnail preview immediately
  3. POST to `/api/v1/uploads` with `{ contentType: file.type }` to get `{ uploadUrl, key }`
  4. PUT the file directly to `uploadUrl` (no auth headers — presigned URL includes credentials)
  5. On HTTP 200: call `onPhotoChange(key)`, show thumbnail (80x80, rounded-md, object-cover)
  6. On failure: show error "Photo upload failed. Try again." in 14px destructive color
- Thumbnail preview with X remove button overlaid top-right
- During upload: spinner overlay on the thumbnail
- Error state: destructive border + error message below

**5. `apps/web/components/meal-type-toggle.tsx`** — per UI-SPEC Component #1:
- Props: `value: 'restaurant' | 'homemade'`, `onChange: (value: 'restaurant' | 'homemade') => void`
- Segmented control with two options: "Restaurant" and "Homemade"
- Selected segment: accent bg (#F97316), white text (#FFFFFF), font-semibold (600)
- Unselected segment: surface bg (#FFF0E0), text-primary (#1C1917), font-normal (400)
- Height: 44px, full width of form column, border-radius: 12px (radii.lg)
- Render as two `<button>` elements side by side inside a container div
  </action>
  <verify>
    <automated>cd /Users/sydneycole/Desktop/lunchboxd && pnpm --filter web type-check</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/star-rating.tsx contains `'use client'`
    - apps/web/components/star-rating.tsx contains `role="radiogroup"`
    - apps/web/components/star-rating.tsx contains `#F97316` (accent fill for selected stars)
    - apps/web/components/tag-input.tsx contains `'use client'`
    - apps/web/components/tag-input.tsx contains `Add a tag`
    - apps/web/components/restaurant-search.tsx contains `'use client'`
    - apps/web/components/restaurant-search.tsx contains `api/v1/restaurants/search`
    - apps/web/components/restaurant-search.tsx contains `role="listbox"`
    - apps/web/components/restaurant-search.tsx contains `Add manually` or `manually`
    - apps/web/components/photo-picker.tsx contains `'use client'`
    - apps/web/components/photo-picker.tsx contains `api/v1/uploads`
    - apps/web/components/photo-picker.tsx contains `10` (10MB limit reference)
    - apps/web/components/meal-type-toggle.tsx contains `'use client'`
    - apps/web/components/meal-type-toggle.tsx contains `restaurant` and `homemade`
    - pnpm --filter web type-check exits 0
  </acceptance_criteria>
  <done>Five input components built with correct visual specs, accessibility attributes, and API integrations. StarRating supports half-star selection with keyboard. RestaurantSearch debounces and shows "add manually" only when results are empty. PhotoPicker uploads via presigned URL flow.</done>
</task>

<task type="auto">
  <name>Task 2: Build ReviewComposer form and /reviews/new page route</name>
  <files>apps/web/components/review-composer.tsx, apps/web/app/(app)/reviews/new/page.tsx</files>
  <read_first>
    - apps/web/components/star-rating.tsx (Task 1 output — confirm export name and props interface)
    - apps/web/components/tag-input.tsx (Task 1 output — confirm export name and props interface)
    - apps/web/components/restaurant-search.tsx (Task 1 output — confirm export name and props interface)
    - apps/web/components/photo-picker.tsx (Task 1 output — confirm export name and props interface)
    - apps/web/components/meal-type-toggle.tsx (Task 1 output — confirm export name and props interface)
    - apps/web/app/(app)/layout.tsx (confirm auth gate — Clerk auth() redirects unauthenticated users)
    - packages/shared/src/schemas/index.ts (reviewSchema for client-side validation before submit)
    - .planning/phases/02-reviews-meals/02-UI-SPEC.md (Screen A: Review Composer spec — exact field order and spacing)
  </read_first>
  <action>
**1. `apps/web/components/review-composer.tsx`** — per UI-SPEC Screen A:
- `'use client'` directive
- Props: `mode: 'create' | 'edit'`, `initialData?: Partial<CreateReviewInput> & { id?: string }`, `onSuccess?: () => void`
- Use React `useState` for each field: mealType (default 'restaurant'), rating, note, restaurantId (with restaurant name for display), photoKey, tags, mealDate (default today as YYYY-MM-DD string)
- Form layout — single scrollable column, max-w-[600px] mx-auto, bg (#FFF8F0), py-8 (xl) px-4 (md):
  1. Page title: "New Review" (create mode) or "Edit Review" (edit mode) — 20px/semibold/Inter/text-primary
  2. `<MealTypeToggle value={mealType} onChange={setMealType} />`
  3. `<RestaurantSearch value={restaurant} onChange={setRestaurant} />` — hidden when mealType === 'homemade' (D-06)
  4. `<StarRating value={rating} onChange={setRating} />`
  5. Textarea with label "What did you think?" — 4 rows min, max 2000 chars, counter "X / 2000" shown at 1500+ chars
  6. `<PhotoPicker photoKey={photoKey} onPhotoChange={setPhotoKey} />`
  7. `<TagInput tags={tags} onChange={setTags} />`
  8. Date input with label "Meal date" — `<input type="date">`, 44px, full width, default today
  9. Submit button: "Post Review" (create) or "Save Changes" (edit) — full width, 44px, accent bg, white text, semibold
- Gap between each field block: 24px (lg spacing)
- Validation on submit:
  - Rating is required — show "Please add a rating." error below StarRating if missing
  - All other fields optional
  - Use reviewSchema.safeParse() for full validation
  - First error field receives focus
- Submit handler:
  - Create mode: POST /api/v1/reviews with the form data
  - Edit mode: PATCH /api/v1/reviews/{id} with the form data
  - On success: call `onSuccess` callback (or `router.push('/reviews')`)
  - On error: show "Something went wrong. Please try again." as a general form error
- Loading state: Loader2 spinner in submit button, button disabled
- When mealType is 'homemade': set restaurantId to null, hide RestaurantSearch (D-06)

**2. `apps/web/app/(app)/reviews/new/page.tsx`**:
- Server Component wrapper that renders `<ReviewComposer mode="create" />`
- Import ReviewComposer from '@/components/review-composer'
- The (app) layout already gates auth via Clerk
- No additional metadata needed for now
  </action>
  <verify>
    <automated>cd /Users/sydneycole/Desktop/lunchboxd && pnpm --filter web type-check</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/review-composer.tsx contains `'use client'`
    - apps/web/components/review-composer.tsx contains `import.*StarRating`
    - apps/web/components/review-composer.tsx contains `import.*TagInput`
    - apps/web/components/review-composer.tsx contains `import.*RestaurantSearch`
    - apps/web/components/review-composer.tsx contains `import.*PhotoPicker`
    - apps/web/components/review-composer.tsx contains `import.*MealTypeToggle`
    - apps/web/components/review-composer.tsx contains `api/v1/reviews`
    - apps/web/components/review-composer.tsx contains `Post Review`
    - apps/web/components/review-composer.tsx contains `Save Changes`
    - apps/web/components/review-composer.tsx contains `Please add a rating`
    - apps/web/components/review-composer.tsx contains `mealType === 'homemade'` or similar conditional hiding
    - apps/web/app/(app)/reviews/new/page.tsx contains `ReviewComposer`
    - pnpm --filter web type-check exits 0
  </acceptance_criteria>
  <done>ReviewComposer form wires all five input components into a single-page scrollable form per D-01. The /reviews/new page renders the composer within the authenticated (app) layout. Validation enforces required rating and optional other fields. Submit posts to API and redirects on success.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web type-check` passes
- All 7 component/page files exist
- ReviewComposer imports and renders all 5 input components
- RestaurantSearch is conditionally hidden when mealType is 'homemade'
- Submit calls POST /api/v1/reviews (create) or PATCH /api/v1/reviews/:id (edit)
- Accessibility: role attributes on StarRating and RestaurantSearch
</verification>

<success_criteria>
- User can navigate to /reviews/new and see the full composer form
- All five input components render with correct design tokens and interaction states
- Meal type toggle controls restaurant search visibility per D-03 and D-06
- Restaurant search debounces, shows results, and offers "add manually" only on empty results per D-04 and D-05
- Photo upload uses presigned URL flow per RESEARCH.md
- Form validates rating as required and submits to API
</success_criteria>

<output>
After completion, create `.planning/phases/02-reviews-meals/02-04-SUMMARY.md`
</output>
