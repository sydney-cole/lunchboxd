---
phase: quick
plan: 260429-a01
type: execute
wave: 1
depends_on: []
files_modified:
  # Wave 1 — mobile removal
  - pnpm-workspace.yaml
  - turbo.json
  - app.json
  - eas.json
  - apps/mobile/  # deleted entirely
  # Wave 2 — API route fixes
  - apps/web/app/api/v1/webhooks/clerk/route.ts
  - apps/web/app/api/v1/follows/route.ts
  - apps/web/app/api/v1/likes/route.ts
  - apps/web/app/api/v1/restaurants/search/route.ts
  - apps/web/app/api/v1/restaurants/map/route.ts
  - apps/web/app/api/v1/reviews/[id]/route.ts
  - apps/web/app/api/v1/reviews/route.ts
  - apps/web/app/api/v1/users/route.ts
  - apps/web/app/api/v1/notifications/route.ts
  - apps/web/app/api/v1/users/[username]/followers/route.ts
  - apps/web/app/api/v1/users/[username]/following/route.ts
  - apps/web/app/api/v1/uploads/route.ts
  # Wave 3 — schema/lib fixes
  - apps/web/lib/schema.ts
  - apps/web/lib/queries.ts
  - apps/web/lib/db.ts
  - apps/web/lib/utils.ts
  # Wave 4 — component fixes
  - apps/web/components/follow-button.tsx
  - apps/web/components/notification-bell.tsx
  - apps/web/components/tag-input.tsx
  - apps/web/components/review-card.tsx
  - apps/web/components/photo-picker.tsx
  - apps/web/components/review-composer.tsx
  # Wave 5 — page fixes
  - apps/web/app/(app)/[username]/page.tsx
  - apps/web/app/(app)/page.tsx
  - apps/web/app/(app)/map/page.tsx
  - apps/web/app/(app)/reviews/[id]/edit/page.tsx
  - apps/web/app/setup-username/page.tsx
autonomous: true
requirements: [quick-task]

must_haves:
  truths:
    - "apps/mobile directory is deleted; pnpm-workspace.yaml and turbo.json have no mobile references"
    - "POST /api/v1/follows returns 200 only once; repeated POST does not increment counters"
    - "DELETE /api/v1/follows does not decrement stats when no follow row existed"
    - "GET /api/v1/reviews/:id returns review data for any authenticated user, not just owner"
    - "POST /api/v1/users returns 409 when username is already taken"
    - "POST /api/v1/likes checks review existence before inserting like row"
    - "fanOutToFollowers uses onConflictDoNothing; feedItems has unique index on (ownerUserId, reviewId)"
    - "webhooks/clerk user.delete wraps in try/catch, removes dependent rows before user row"
    - "webhooks/clerk user.updated only sets avatarUrl from Clerk when current avatarUrl is null"
    - "FollowButton tracks local state via useState; initialState updates reflected immediately after mutation"
    - "Profile page onEdit/onDelete handlers navigate to edit page and trigger delete dialog"
    - "Feed page onEdit/onDelete handlers navigate to edit page and trigger delete dialog"
    - "tag-input.tsx normalizes tags to lowercase before deduplication check"
    - "photo-picker.tsx edit mode passes null photoKey, not the existing photo URL"
    - "formatRelativeTime handles negative diff gracefully (shows 'just now')"
    - "map/page.tsx guards for missing NEXT_PUBLIC_GOOGLE_MAPS_API_KEY with an error state"
    - "notifications/route.ts filters soft-deleted reviews with isNull(reviews.deletedAt)"
    - "followers and following routes include .orderBy(asc(users.id))"
    - "reviews route.ts (POST) increments userStats.reviewCount; reviews/[id] (DELETE) decrements it"
    - "restaurants/map/route.ts gets one row per restaurant (not one row per reviewer)"
    - "notification-bell.tsx invalidates ['notifications'] query after mark-all-read"
    - "db.ts has explicit DATABASE_URL guard before neon() call"
    - "uploads route.ts rejects files over 10MB server-side"
    - "setup-username page enforces minimum 2-char username"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace config with mobile removed"
    - path: "apps/web/lib/schema.ts"
      provides: "feedItems unique index on (ownerUserId, reviewId)"
    - path: "apps/web/lib/queries.ts"
      provides: "fanOutToFollowers with onConflictDoNothing"
    - path: "apps/web/lib/db.ts"
      provides: "Explicit DATABASE_URL guard"
    - path: "apps/web/lib/utils.ts"
      provides: "formatRelativeTime handles negative diff"
  key_links:
    - from: "apps/web/app/api/v1/follows/route.ts"
      to: "userStats upserts"
      via: "if (inserted.length > 0) gate"
      pattern: "inserted\\.length > 0"
    - from: "apps/web/components/review-composer.tsx"
      to: "apps/web/app/(app)/reviews/[id]/edit/page.tsx"
      via: "initialData.photoKey must be null in edit mode"
      pattern: "photoKey.*null"
    - from: "apps/web/lib/queries.ts"
      to: "apps/web/lib/schema.ts"
      via: "onConflictDoNothing matches feedItemsUniqueIdx"
      pattern: "onConflictDoNothing"
---

<objective>
Strip the mobile app from the monorepo and fix all web bugs identified in REVIEW.md.

Purpose: Mobile app is being removed to focus on web-only. The web codebase has ~20 correctness and UX bugs ranging from critical data corruption (counter drift, orphaned likes) to broken UI flows (no-op edit/delete handlers, wrong photo key format).

Output:
- apps/mobile deleted; workspace configs cleaned
- All CRITICAL (CR-03 to CR-10), HIGH (HI-01 to HI-06, HI-08, HI-09), MEDIUM (ME-01, ME-02, ME-04, ME-05, ME-07, ME-08, ME-09), and LOW (LO-01 to LO-03, LO-06, LO-07) bugs resolved
- Skipped: CR-01, CR-02, HI-07, LO-04, LO-05 (mobile-only or covered by mobile removal), ME-03 (informational, no fix specified), ME-06 (race condition, documented only)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sydneycole/Desktop/lunchboxd/.planning/STATE.md
@/Users/sydneycole/Desktop/lunchboxd/REVIEW.md
@/Users/sydneycole/Desktop/lunchboxd/apps/web/lib/schema.ts
@/Users/sydneycole/Desktop/lunchboxd/apps/web/lib/queries.ts

IMPORTANT: Before editing any Next.js files, read the relevant guide in
`apps/web/node_modules/next/dist/docs/` — this project uses Next.js 16 which has
breaking changes from earlier versions. Also check `apps/web/AGENTS.md` for
project-specific conventions.
</context>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 1 — Mobile removal
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 1: Strip mobile app from monorepo</name>
  <files>
    pnpm-workspace.yaml,
    turbo.json,
    app.json,
    eas.json,
    apps/mobile/ (delete)
  </files>
  <action>
    1. Delete the apps/mobile directory entirely:
       `rm -rf /Users/sydneycole/Desktop/lunchboxd/apps/mobile`

    2. Edit pnpm-workspace.yaml — the current content is:
       ```
       packages:
         - 'apps/*'
         - 'packages/*'
       ```
       The glob `apps/*` will now only match `apps/web` since mobile is gone.
       No change needed to pnpm-workspace.yaml itself (glob approach is fine),
       but verify mobile is gone so the glob does not pick it up.

    3. Edit turbo.json — remove any mobile-specific tasks or env vars that only
       apply to Expo. The current turbo.json has no explicit mobile-only tasks;
       all tasks (build, dev, lint, type-check, test) are workspace-generic.
       No changes needed to turbo.json unless mobile-specific entries are present.

    4. Delete eas.json at repo root:
       `rm /Users/sydneycole/Desktop/lunchboxd/eas.json`

    5. Edit app.json at repo root — current content has only an `expo` key with
       EAS project ID. Since this is an Expo-only file, delete it:
       `rm /Users/sydneycole/Desktop/lunchboxd/app.json`

    6. Check for a `mobile/` symlink at repo root and remove if it exists:
       `[ -L /Users/sydneycole/Desktop/lunchboxd/mobile ] && rm /Users/sydneycole/Desktop/lunchboxd/mobile`

    7. Run `pnpm install` from repo root to clean up the lockfile now that
       the mobile workspace is gone.
  </action>
  <verify>
    <automated>
      ls /Users/sydneycole/Desktop/lunchboxd/apps/ | grep -v mobile &&
      [ ! -d /Users/sydneycole/Desktop/lunchboxd/apps/mobile ] &&
      [ ! -f /Users/sydneycole/Desktop/lunchboxd/eas.json ] &&
      [ ! -f /Users/sydneycole/Desktop/lunchboxd/app.json ] &&
      echo "Mobile stripped OK"
    </automated>
  </verify>
  <done>apps/mobile does not exist; eas.json and app.json deleted; pnpm-lock.yaml updated without mobile packages</done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 2 — API route bug fixes
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 2: Fix webhook, follows, likes, and users API routes (CR-03 to CR-05, CR-09, CR-10, LO-06)</name>
  <files>
    apps/web/app/api/v1/webhooks/clerk/route.ts,
    apps/web/app/api/v1/follows/route.ts,
    apps/web/app/api/v1/likes/route.ts,
    apps/web/app/api/v1/users/route.ts,
    apps/web/app/setup-username/page.tsx
  </files>
  <action>
    Read each file fully before editing. Apply these fixes:

    **webhooks/clerk/route.ts (CR-03, LO-06):**
    - For `user.deleted` event: wrap the delete block in try/catch. Before deleting
      the users row, delete dependent rows in order:
      1. Delete notifications where userId = that user's id OR actorId = that user's id
      2. Delete likes where userId = that user's id
      3. Delete feedItems where ownerUserId = that user's id
      4. Delete reviewTags where reviewId IN (SELECT id FROM reviews WHERE userId = ...)
         — do this by first selecting review IDs for the user, then deleting tags
      5. Soft-delete reviews where userId = that user's id (set deletedAt = now())
         rather than hard-deleting, to preserve social history
      6. Delete follows where followerId = id OR followeeId = id
      7. Delete friendships where userAId = id OR userBId = id
      8. Delete userStats where userId = id
      9. Finally delete the users row
      Wrap the whole thing in a try/catch; on error log and return 500 with JSON error body.
    - For `user.updated` event (LO-06): only update avatarUrl from Clerk's
      `event.data.image_url` when the current user's `avatarUrl` in the DB is null.
      Add a SELECT before the UPDATE to read the current avatarUrl, then conditionally
      include it in the set clause. Alternatively: use a raw SQL expression in the
      update: `SET avatar_url = COALESCE(avatar_url, $clerk_image_url)`.
      With Drizzle: use `sql\`COALESCE(${users.avatarUrl}, ${imageUrl})\`` in the set.

    **follows/route.ts (CR-04, CR-05):**
    - POST handler: gate the two userStats upserts (followingCount and followerCount)
      inside `if (inserted.length > 0) { ... }`. They are currently unconditional.
      The notification insert is already gated — the stats upserts must be too.
    - DELETE handler: add `.returning({ id: follows.id })` to the `db.delete(follows)`
      call. Store result as `deleted`. Gate the stats decrements inside
      `if (deleted.length > 0) { ... }`.

    **likes/route.ts (CR-10):**
    - Move the review existence check to BEFORE the `db.insert(likes)` call.
      Current order: insert like → fetch review → 404 if not found (like orphaned).
      New order: fetch review → 404 if not found → insert like.
      The `onConflictDoNothing()` on the insert remains; only the order changes.

    **users/route.ts (CR-09):**
    - Wrap the `db.insert(users)` call (and the onConflictDoUpdate) in a try/catch.
      In the catch block, check if the error has a `code` property equal to `'23505'`
      (PostgreSQL unique constraint violation). If so, return:
      `NextResponse.json({ error: 'Username already taken' }, { status: 409 })`
      Otherwise rethrow or return 500.

    **setup-username/page.tsx (ME-07):**
    - Add minimum length validation of 2 characters to the client-side validation
      that runs before the API call. The regex check currently allows single-char
      usernames. Add: `if (trimmed.length < 2) { setError('Username must be at least 2 characters'); return }`.
      Also add `minLength: 2` to the input field attributes.
  </action>
  <verify>
    <automated>cd /Users/sydneycole/Desktop/lunchboxd/apps/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compiles without errors in affected files. Manual spot-check:
    follows/route.ts has `if (inserted.length > 0)` wrapping stats upserts.
    webhooks/clerk/route.ts has try/catch around user.deleted block with dependent row
    cleanup before user row deletion. likes/route.ts checks review before inserting like.
    users/route.ts catches error code 23505 and returns 409.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix restaurants, reviews, notifications, users-list API routes (CR-07, CR-08, HI-04 prep, HI-05, HI-08 partial, HI-09, ME-08, ME-09)</name>
  <files>
    apps/web/app/api/v1/restaurants/search/route.ts,
    apps/web/app/api/v1/restaurants/map/route.ts,
    apps/web/app/api/v1/reviews/[id]/route.ts,
    apps/web/app/api/v1/reviews/route.ts,
    apps/web/app/api/v1/notifications/route.ts,
    apps/web/app/api/v1/users/[username]/followers/route.ts,
    apps/web/app/api/v1/users/[username]/following/route.ts,
    apps/web/app/api/v1/uploads/route.ts
  </files>
  <action>
    Read each file fully before editing. Apply these fixes:

    **restaurants/search/route.ts (CR-08, ME-06 comment):**
    - After the `Promise.all(places.slice(0, 5).map(...))`, add a filter to remove
      undefined results before using the array:
      `const upserted = results.filter((r): r is typeof r & {} => r !== undefined)`
      or simply `const upserted = results.filter(Boolean)` then cast to the non-null type.
    - Add a comment above the cache-check block noting the race condition risk:
      // NOTE: Two concurrent requests for the same query can both miss the cache
      // and call Google Places. The onConflictDoUpdate handles DB-level duplicates,
      // but the combined response may briefly return duplicate-looking rows from
      // two concurrent in-flight results. A distributed lock would be needed to
      // eliminate this; acceptable at MVP scale.

    **restaurants/map/route.ts (HI-09):**
    - The current query uses `selectDistinct` with `reviewUserId`, meaning it returns
      one row per (restaurant, reviewer) pair instead of one row per restaurant.
    - Restructure the query to get one row per restaurant. Use a subquery approach:
      First, select distinct restaurant IDs that have been reviewed by followed users
      (or the user themselves), then join back to restaurants for the full row.
      In Drizzle, you can use `.groupBy(restaurants.id, ...)` to collapse duplicates:
      ```ts
      const reviewedRows = await db
        .select({
          id: restaurants.id,
          name: restaurants.name,
          lat: restaurants.lat,
          lng: restaurants.lng,
        })
        .from(restaurants)
        .innerJoin(reviews, eq(reviews.restaurantId, restaurants.id))
        .where(
          and(
            isNotNull(restaurants.lat),
            isNotNull(restaurants.lng),
            isNull(reviews.deletedAt),
            // existing filter for followed users or own reviews
          )
        )
        .groupBy(restaurants.id, restaurants.name, restaurants.lat, restaurants.lng)
        .limit(500)
      ```
      Remove the post-query JS deduplication loop since groupBy handles it.
      Update the response shape accordingly (no reviewUserId in the output).

    **reviews/[id]/route.ts (CR-07):**
    - In the GET handler, remove the `eq(reviews.userId, userId)` filter so any
      authenticated user can fetch a review by ID. The query should be:
      `.where(and(eq(reviews.id, id), isNull(reviews.deletedAt)))`
    - Keep the userId ownership check for PUT/PATCH/DELETE handlers — only the owner
      can modify/delete their own review.

    **reviews/route.ts (ME-09 — reviewCount increment):**
    - In the POST handler, after successfully creating a review and calling
      `fanOutToFollowers`, add a userStats upsert to increment reviewCount:
      ```ts
      await db.insert(userStats)
        .values({ userId, reviewCount: '1' })
        .onConflictDoUpdate({
          target: userStats.userId,
          set: {
            reviewCount: sql`${userStats.reviewCount} + 1`,
            updatedAt: new Date(),
          },
        })
      ```
      Import userStats and sql from the appropriate modules if not already imported.

    **reviews/[id]/route.ts (ME-09 — reviewCount decrement):**
    - In the DELETE handler (soft-delete), after setting deletedAt, add a userStats
      update to decrement reviewCount:
      ```ts
      await db.insert(userStats)
        .values({ userId, reviewCount: '0' })
        .onConflictDoUpdate({
          target: userStats.userId,
          set: {
            reviewCount: sql`GREATEST(${userStats.reviewCount} - 1, 0)`,
            updatedAt: new Date(),
          },
        })
      ```

    **notifications/route.ts (ME-08):**
    - In the review fetch for like notifications, add `isNull(reviews.deletedAt)`
      to the WHERE clause so soft-deleted reviews' restaurant names are excluded.

    **users/[username]/followers/route.ts and following/route.ts (HI-05):**
    - Add `.orderBy(asc(users.id))` to both queries to ensure deterministic
      cursor-based pagination. Import `asc` from 'drizzle-orm' if not already present.

    **uploads/route.ts (ME-01 — server-side file size check):**
    - After extracting the file from formData, add a size check before calling put():
      ```ts
      const MAX_SIZE = 10 * 1024 * 1024 // 10MB
      if (file.size > MAX_SIZE) {
        return NextResponse.json({ error: 'File too large. Maximum size is 10MB.' }, { status: 413 })
      }
      ```
  </action>
  <verify>
    <automated>cd /Users/sydneycole/Desktop/lunchboxd/apps/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compiles without errors. restaurants/map/route.ts uses groupBy instead of
    selectDistinct with reviewUserId. reviews/[id] GET has no userId filter.
    followers/following routes have .orderBy(asc(users.id)). reviews route POST
    increments reviewCount; reviews/[id] DELETE decrements it. uploads route rejects
    files over 10MB.
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 3 — Schema and lib fixes
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 4: Schema unique index, fanOut idempotency, db guard, utils fix (HI-04, ME-02, LO-07)</name>
  <files>
    apps/web/lib/schema.ts,
    apps/web/lib/queries.ts,
    apps/web/lib/db.ts,
    apps/web/lib/utils.ts
  </files>
  <action>
    **schema.ts (HI-04 — feedItems unique index):**
    - Add a unique index on (ownerUserId, reviewId) to the feedItems table definition.
      The table currently only has `feedItemsOwnerIdx` (single-column on ownerUserId).
      Add inside the pgTable callback:
      ```ts
      feedItemsUniqueIdx: uniqueIndex('feed_items_unique_idx').on(table.ownerUserId, table.reviewId),
      ```
      Note: Do NOT create a migration file — the schema change is for future migrations
      only. The unique index definition in schema.ts is the source of truth.
      Add a comment: // Unique constraint enables onConflictDoNothing in fanOutToFollowers (HI-04)

    **queries.ts (HI-04 — fanOut idempotency):**
    - On the `db.insert(feedItems).values(feedRows)` call, add `.onConflictDoNothing()`:
      ```ts
      await db.insert(feedItems).values(feedRows).onConflictDoNothing()
      ```

    **db.ts (ME-02 — explicit DATABASE_URL guard):**
    - Replace:
      `const sql = neon(process.env.DATABASE_URL!)`
      With:
      ```ts
      if (!process.env.DATABASE_URL) {
        throw new Error('Missing required environment variable: DATABASE_URL')
      }
      const sql = neon(process.env.DATABASE_URL)
      ```

    **utils.ts (LO-07 — negative diff in formatRelativeTime):**
    - After computing `const diff = Date.now() - new Date(isoString).getTime()`,
      add: `if (diff < 0) return 'just now'`
      This handles clock skew or future timestamps gracefully.
  </action>
  <verify>
    <automated>cd /Users/sydneycole/Desktop/lunchboxd/apps/web && npx tsc --noEmit 2>&1 | head -20 && grep -n "onConflictDoNothing" /Users/sydneycole/Desktop/lunchboxd/apps/web/lib/queries.ts && grep -n "feed_items_unique_idx" /Users/sydneycole/Desktop/lunchboxd/apps/web/lib/schema.ts && grep -n "DATABASE_URL" /Users/sydneycole/Desktop/lunchboxd/apps/web/lib/db.ts && echo "All schema/lib fixes verified"</automated>
  </verify>
  <done>
    feedItems table in schema.ts has feedItemsUniqueIdx on (ownerUserId, reviewId).
    fanOutToFollowers in queries.ts uses .onConflictDoNothing().
    db.ts throws descriptive error when DATABASE_URL is missing.
    formatRelativeTime returns 'just now' for negative diff.
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 4 — Component fixes
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 5: Fix web components — follow button, notification bell, tag input, review card, photo picker, review composer (HI-01, HI-08, LO-01, LO-02, ME-05, ME-04/LO-05)</name>
  <files>
    apps/web/components/follow-button.tsx,
    apps/web/components/notification-bell.tsx,
    apps/web/components/tag-input.tsx,
    apps/web/components/review-card.tsx,
    apps/web/components/photo-picker.tsx,
    apps/web/components/review-composer.tsx
  </files>
  <action>
    Read each file fully before editing.

    **follow-button.tsx (HI-01):**
    - Add `useState(initialState)` to track local follow state:
      ```ts
      const [currentState, setCurrentState] = useState(initialState)
      ```
    - In the `onSuccess` callback of the follow mutation, update `currentState`:
      Set it to the followState returned from the API response (e.g., 'following' or
      'friends' after following; 'none' or 'following' after unfollowing depending on
      whether it was a mutual follow). Read the existing mutation to see what the API
      returns and update setCurrentState accordingly.
    - Replace all reads of the prop `initialState` used as display with `currentState`.
    - Keep `initialState` as a prop for first-render initialization only.

    **notification-bell.tsx (HI-08):**
    - In `handleOpen`, after the existing `queryClient.invalidateQueries({ queryKey: ['notifications-unread'] })`,
      add: `queryClient.invalidateQueries({ queryKey: ['notifications'] })`
      so the panel list also refetches and shows updated read state.

    **tag-input.tsx (LO-01):**
    - In `commitTag`, change:
      `const trimmed = raw.trim()`
      to:
      `const trimmed = raw.trim().toLowerCase()`
      This ensures "Spicy" and "spicy" are treated as the same tag, matching server
      normalization behavior.

    **review-card.tsx (LO-02):**
    - Replace the single `useEffect(() => { checkClamped() }, [checkClamped])` with
      a ResizeObserver on `bodyRef.current`:
      ```ts
      useEffect(() => {
        const el = bodyRef.current
        if (!el) return
        checkClamped()
        const ro = new ResizeObserver(() => checkClamped())
        ro.observe(el)
        return () => ro.disconnect()
      }, [checkClamped])
      ```
      This rechecks clamping whenever the element's size changes (e.g., window resize).

    **photo-picker.tsx (ME-05):**
    - Fix the useEffect cleanup to use a ref instead of the state value in the closure:
      ```ts
      const previewUrlRef = useRef<string | null>(null)
      // when creating a new object URL:
      const url = URL.createObjectURL(file)
      previewUrlRef.current = url
      setPreviewUrl(url)
      // cleanup effect — revoke only on unmount, not on every previewUrl change:
      useEffect(() => {
        return () => {
          if (previewUrlRef.current) URL.revokeObjectURL(previewUrlRef.current)
        }
      }, [])  // empty deps — runs only on unmount
      ```
      Remove the existing `useEffect([previewUrl])` cleanup pattern.

    **review-composer.tsx (ME-04, LO-05):**
    - Change the initial photoKey state initialization to always start null in edit mode:
      ```ts
      const [photoKey, setPhotoKey] = useState<string | null>(null)
      ```
      (Remove `initialData?.photoKey ?? null` from useState — photoKey always starts null.)
    - Add a separate state to track whether the user has explicitly changed the photo:
      ```ts
      const [photoChanged, setPhotoChanged] = useState(false)
      ```
    - When building the PATCH payload for edit mode, only include `photoKey` if
      `photoChanged` is true. If `photoChanged` is false (user did not touch the photo
      field), omit `photoKey` from the payload so the server's updateReviewSchema
      (which is partial()) treats it as "no change":
      ```ts
      const payload = {
        ...(photoChanged ? { photoKey } : {}),
        // rest of fields
      }
      ```
    - When `onPhotoChange` is called by the PhotoPicker (a new file was picked or
      cleared), set `setPhotoChanged(true)` in addition to `setPhotoKey(key)`.
    - Ensure PhotoPicker receives `existingPhotoUrl={initialData?.photoUrl ?? null}` as
      a display prop (for showing the existing photo thumbnail) without conflating it
      with the upload key. If PhotoPicker does not already have an `existingPhotoUrl`
      prop for display, add one or use a separate preview mechanism.
  </action>
  <verify>
    <automated>cd /Users/sydneycole/Desktop/lunchboxd/apps/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compiles. follow-button.tsx has useState(initialState) and updates state
    in onSuccess. notification-bell.tsx invalidates ['notifications']. tag-input normalizes
    to lowercase. review-card uses ResizeObserver. photo-picker cleanup uses ref.
    review-composer starts photoKey as null and only sends it on change.
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 5 — Page fixes
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 6: Fix web pages — profile, feed, map, edit review, setup username (HI-02, HI-03, HI-06, LO-03, ME-04 edit page)</name>
  <files>
    apps/web/app/(app)/[username]/page.tsx,
    apps/web/app/(app)/page.tsx,
    apps/web/app/(app)/map/page.tsx,
    apps/web/app/(app)/reviews/[id]/edit/page.tsx,
    apps/web/app/setup-username/page.tsx
  </files>
  <action>
    Read each file fully before editing.

    **[username]/page.tsx (HI-02, HI-06):**
    - Replace the no-op `onEdit={() => {}}` and `onDelete={() => {}}` handlers with
      actual implementations:
      - The page likely already has access to a router (via `useRouter`). If not, add it.
      - `onEdit={(id) => router.push(\`/reviews/${id}/edit\`)}`
      - For `onDelete`, add a `deleteTarget` state (`useState<string | null>(null)`),
        set it via `onDelete={(id) => setDeleteTarget(id)}`, and render the existing
        `DeleteDialog` component (already imported from components) with appropriate
        props to handle the delete API call and query invalidation.
    - For HI-06 (follow state): Add a `followState` query to the profile page that
      fetches the current viewer's follow relationship with the profile user.
      Use the existing `/api/v1/users/[username]` response if it includes followState,
      OR add a separate TanStack Query call to fetch it.
      Pass the actual followState to `<FollowButton initialState={followState ?? 'none'} />`.
      If the profile API does not return followState for the viewer, check whether
      the follow enrichment already exists in the user endpoint (it may already be there
      from Phase 3 work — read the [username] API route before deciding). If not present,
      add a follow state lookup in the GET /api/v1/users/[username] route for
      authenticated requests and pass it through the profile page.

    **page.tsx (feed) (HI-03):**
    - Same pattern as HI-02: replace no-op handlers.
    - The feed page renders review cards with `isOwnReview` per item. Wire:
      - `onEdit={(id) => router.push(\`/reviews/${id}/edit\`)}`
      - For `onDelete`, add a `deleteTarget` state and render DeleteDialog. The feed
        uses TanStack InfiniteQuery — after a successful delete, invalidate the ['feed']
        query so the deleted item disappears.

    **map/page.tsx (LO-03):**
    - Add an early return guard for missing API key. Place near the top of the component
      (after hooks, before JSX return):
      ```ts
      if (!process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY) {
        return (
          <div className="flex h-full items-center justify-center p-8 text-center">
            <p className="text-muted-foreground">
              Map unavailable — Google Maps API key not configured.
            </p>
          </div>
        )
      }
      ```
    - Remove the `!` non-null assertion from the `apiKey` prop.

    **reviews/[id]/edit/page.tsx (ME-04):**
    - Change the initialData passed to ReviewComposer so that `photoKey` is `null`
      (not the photo URL):
      ```ts
      initialData={{
        // ... other fields
        photoKey: null,   // was: review.photoUrl ?? null
        // photoUrl remains available for display in the composer
      }}
      ```
    - If the ReviewComposer needs to display the existing photo, pass it separately
      (e.g., as `existingPhotoUrl={review.photoUrl}` if that prop was added in Task 5),
      or verify the composer handles this correctly after the Task 5 changes.

    **setup-username/page.tsx (ME-07 — also partially done in Task 2):**
    - Verify the minimum length validation added in Task 2 (server-side in users/route.ts)
      is also reflected here client-side. The `usernameSchema` in setup-username or
      the shared schema should use `min(2)`. If the schema is defined in this file,
      update `z.string().min(1)` to `z.string().min(2, 'Username must be at least 2 characters')`.
  </action>
  <verify>
    <automated>cd /Users/sydneycole/Desktop/lunchboxd/apps/web && npx tsc --noEmit 2>&1 | head -30 && echo "All page fixes type-check OK"</automated>
  </verify>
  <done>
    TypeScript compiles. Profile page has working onEdit (router.push) and onDelete
    (DeleteDialog). Feed page has working onEdit/onDelete. Map page guards for missing
    API key. Edit review page passes null photoKey. setup-username enforces min 2 chars.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | All API routes require auth(); user-supplied IDs in path/body must be resolved to internal UUIDs server-side |
| Webhook → API | Clerk webhooks verified by SVIX signature; event.data.id is trusted after verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-a01-01 | Tampering | follows/route.ts stats upsert | mitigate | Gate stats increments on inserted.length > 0 (CR-04); gate decrements on deleted.length > 0 (CR-05) — done in Task 2 |
| T-a01-02 | Tampering | likes/route.ts orphan like | mitigate | Check review existence before inserting like (CR-10) — done in Task 2 |
| T-a01-03 | Spoofing | webhooks/clerk user.updated overwrites avatar | mitigate | Use COALESCE to only set avatarUrl from Clerk when current is null (LO-06) — done in Task 2 |
| T-a01-04 | Denial of Service | uploads/route.ts no file size cap | mitigate | Reject files over 10MB server-side before upload (ME-01) — done in Task 3 |
| T-a01-05 | Information Disclosure | reviews/[id] GET blocked for non-owners | mitigate | Remove userId filter from GET so any authenticated user can fetch a review (CR-07) — done in Task 3 |
| T-a01-06 | Tampering | fanOutToFollowers duplicate feed rows | mitigate | Add onConflictDoNothing + unique index (HI-04) — done in Task 4 |
</threat_model>

<verification>
After all tasks complete, run:
```bash
cd /Users/sydneycole/Desktop/lunchboxd/apps/web && npx tsc --noEmit
```

Confirm:
- [ ] No TypeScript errors
- [ ] apps/mobile directory does not exist: `[ ! -d apps/mobile ]`
- [ ] eas.json does not exist at repo root: `[ ! -f eas.json ]`
- [ ] feedItems unique index present in schema.ts: `grep "feed_items_unique_idx" apps/web/lib/schema.ts`
- [ ] onConflictDoNothing in fanOutToFollowers: `grep "onConflictDoNothing" apps/web/lib/queries.ts`
- [ ] DATABASE_URL guard in db.ts: `grep "Missing required" apps/web/lib/db.ts`
- [ ] Review count increment in reviews/route.ts POST: `grep "reviewCount" apps/web/app/api/v1/reviews/route.ts`
- [ ] orderBy in followers route: `grep "orderBy" apps/web/app/api/v1/users/[username]/followers/route.ts`
</verification>

<success_criteria>
- Mobile app fully stripped; workspace builds without mobile references
- All CRITICAL, HIGH, MEDIUM, and LOW web bugs from REVIEW.md resolved
- No TypeScript compilation errors
- No scope reduction: every bug fix specified in the task description is implemented, not deferred
</success_criteria>

<output>
After completion, create `.planning/quick/260429-a01-strip-mobile-fix-bugs/260429-a01-SUMMARY.md`
following the summary template.
</output>
